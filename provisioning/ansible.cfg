[defaults]
inventory = inventory.ini
host_key_checking = False
retry_files_enabled = False

[winrm]
# Guest customization + first-boot specialize can be slow; give WinRM
# generous connection timeouts rather than flapping retries.
connection_timeout = 60
