---
# Copy to group_vars/all/vault.yml and encrypt it:
#   ansible-vault encrypt group_vars/all/vault.yml
# Never commit an unencrypted vault.yml. Everything in group_vars/all/ is
# auto-loaded for every play, so these vault_<name> values are available
# to reference from group_vars/all/vars.yml (or -e on the command line)
# without a vars_files: entry. Never inline a real secret in a playbook.

# ESXi/vCenter API credentials (community.vmware modules, scripts/vmware_guestexec.py)
vault_esxi_hostname: "192.168.99.25"
vault_esxi_username: "root"
vault_esxi_password: "changeme"

# Local admin credentials on the template source VM / clones, used for
# guest-ops during prep and WinRM after clone (over the OOB NIC only).
vault_template_guest_user: "changeme"
vault_template_guest_password: "changeme"

# Directory Services Restore Mode (DSRM) password for the new forest --
# required by dcpromo, needed again for any future authoritative restore.
vault_dsrm_password: "changeme"

# Domain-join credentials (win_domain_membership, add_windows_user.yml) --
# needs real "join a computer to the domain" rights. Distinct from
# vault_template_guest_user/password above (guest-ops on the DC template,
# not the domain) and vault_provisioning_password below (the shared WinRM
# provisioning account) -- keep this one scoped to just domain-join rather
# than reusing a broader credential.
vault_domain_join_user: "changeme"
vault_domain_join_password: "changeme"

# Service-account passwords (build_enterprise_structure.yml). svc_sql and
# svc_web are deliberately weak/stale on purpose -- see
# roles/dc_enterprise_structure/tasks/main.yml and DEVELOPER_NOTES.md for
# why. Still real values here, not literally "changeme" in a live range --
# a red team searching for a genuinely bad password should find one that
# looks stale/reused, not a placeholder.
vault_svc_sql_password: "changeme"
vault_svc_web_password: "changeme"
vault_svc_backup_password: "changeme"
